<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- not selected -->
    android:drawable="@drawable/cast_mute"/>
    <!-- selected -->
    <item android:drawable="@drawable/cast_muted"
          android:state_selected="true"/>
    <!-- default -->
    <item android:drawable="@drawable/cast_mute"/>
</selector>
